Installation

npm install ppllm -g

Use like this:

For learning:

ppllm --help
ppllm generate --help

For using:

ppllm

For using in node.js project:

ppllm -p node

and then next time

ppllm

because settings are stored in local file: ppllm.config.json

MORE READABLE README SOON! ＼(＾▽＾)／